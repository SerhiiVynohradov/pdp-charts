<%= turbo_stream.replace("progress_column_#{item_progress_column.id}") do %>
  <%= render partial: "shared/item_progress_columns/header_cell",
             locals: {
               item_progress_column: item_progress_column,
               superadmin: @superadmin,
               company: @company,
               team: @team,
               user: @user,
               read_only_mode: false  # или по логике
             }
  %>
<% end %>
