<div class="max-w-md mx-auto mt-10 bg-gray-900 p-6 rounded shadow-md">
  <h1 class="text-xl font-bold text-white mb-4">
    <%= t('team_settings.header') %>
  </h1>

  <%= form_with model: @team, url: team_path(@team.company, @team), method: :patch, local: true do |f| %>
    <div class="mb-4">
      <%= f.label :name, t('items_index.name'), class: "block text-gray-400" %>
      <%= f.text_field :name, class: "w-full mt-1 p-2 bg-gray-800 text-white rounded border border-gray-700" %>
    </div>

    <div class="mb-4 flex items-center">
      <%= f.check_box :charts_visible, class: "h-4 w-4 text-blue-600" %>
      <%= f.label :charts_visible,
                  t('team_settings.charts_visible_label'),
                  class: "ml-2 text-gray-400" %>
    </div>

    <div class="mb-4 flex items-center">
      <%= f.check_box :status, class: "h-4 w-4 text-blue-600" %>
      <%= f.label :status,
                  t('team_settings.active_question'),
                  class: "ml-2 text-gray-400" %>
    </div>

    <div class="mb-4">
      <%= f.label :effort_line, t('team_settings.effort_line_label'), class: "block text-gray-400" %>
      <%= f.number_field :effort_line, class: "w-full mt-1 p-2 bg-gray-800 text-white rounded border border-gray-700" %>
    </div>

    <div class="mb-4">
      <%= f.label :wa_line, t('team_settings.wa_line_label'), class: "block text-gray-400" %>
      <%= f.number_field :wa_line, class: "w-full mt-1 p-2 bg-gray-800 text-white rounded border border-gray-700" %>
    </div>

    <div class="mb-4">
      <%= f.label :items_line, t('team_settings.items_line_label'), class: "block text-gray-400" %>
      <%= f.number_field :items_line, class: "w-full mt-1 p-2 bg-gray-800 text-white rounded border border-gray-700" %>
    </div>

    <%= f.submit t('team_settings.update_team_button'),
                 class: "bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded" %>
  <% end %>
</div>
