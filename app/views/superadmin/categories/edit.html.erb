<%= content_for :page_title, "Edit Category" %>
<div class="bg-gray-900 text-white min-h-screen p-6">
  <h1 class="text-2xl font-bold mb-4"><%= t('buttons.edit_category') %></h1>

  <div class="mb-6">
    <%= render partial: "form", locals: { category: @category } %>
  </div>

  <div>
    <%= link_to t('buttons.back_to_categories'), superadmin_categories_path,
                class: "bg-gray-600 hover:bg-gray-700 text-white px-3 py-1 rounded" %>
  </div>
</div>
