<h1 class="text-2xl mb-4">Recommended Items for Team: <%= @team.name %></h1>

<!-- A table or partial that shows recommended items.
     Possibly similar to your Item table, minus status/progress. -->

<table class="min-w-full border-collapse bg-gray-800 text-gray-50">
  <thead>
    <tr>
      <th class="px-4 py-2 text-left">Name</th>
      <th class="px-4 py-2 text-left">Description</th>
      <!-- etc. -->
      <th class="px-4 py-2">Actions</th>
    </tr>
  </thead>
  <tbody>
    <!-- loop recommended items -->
  </tbody>
</table>

<%= link_to "Add New Recommendation", new_recommended_item_path(@team), class: "bg-green-600 px-3 py-1 rounded" %>
